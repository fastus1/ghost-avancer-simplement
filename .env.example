# Ghost Docker Stack Environment Variables
# Copy to .env and populate with generated values

# MySQL Root Password (generate with: openssl rand -hex 32)
MYSQL_ROOT_PASSWORD=

# Ghost Database User Password (generate with: openssl rand -hex 32)
GHOST_DB_PASSWORD=
